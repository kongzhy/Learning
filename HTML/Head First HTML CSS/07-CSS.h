CSS包含一些简单语句，称为规则，每个规则为选择的元素提供样式，典型的规则包括一个选择器，以及一个或多个属性和值
选择器指定规则将应用到哪些元素，通过用逗号分隔元素名，可以一次选择多个元素
在CSS中把注释包围在/*和*/之间，属性以分号结束
在HTML中包含CSS，可以使用<style>标记，也可以使用<link>链接到一个外部样式表
可以使用class属性将元素增加到一个类
可以通过在元素名和类名之间加一个.选择该类中的特定元素，可以使用.classname选择该类的所有元素
元素可以加入多个类，把各个类名放在class属性中，各个类名之间用空格分隔，类名的顺序不重要
首先查看选择器，然后考虑继承，其后考虑默认值
如果多个选择器选择一个元素，则使用最特定的规则，如特定程度相同，使用CSS文件中最后列出的规则
