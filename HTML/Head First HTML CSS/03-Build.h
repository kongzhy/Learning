<h1>、<p>、<blockquote>、<ol>、<ul>、<li>都是块元素，单独显示，在内容前后分别有一个换行
<a>、<q>、<em>、<img>是内联元素，内容与其他内容一起显示
块元素特立独行、内联元素随波逐流。
void元素没有内容(确切的讲，void元素是在开始标记和结束标记之间没有任何内容的元素)，只有一个标记组成
ol(ordered list) ul(unordered list) li(list item)
<ol>和<ul>元素被设计为只能包含<li>元素，可以在列表中建立嵌套列表，将<ol>或<ul>元素放在<li>元素中
要对HTML内容中特殊字符使用字符实体
