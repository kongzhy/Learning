1、<h1>、<p>、<blockquote>、<ol>、<ul>、<li>都是块元素，单独显示，在内容前后分别有一个换行。
2、<a>、<q>、<em>是内联元素，内容与其他内容一起显示。
3、块元素特立独行、内联元素随波逐流。
4、void元素没有内容，只有一个标记组成。
5、ol(ordered list) ul(unordered list) li(list item)
6、<ol>和<ul>元素被设计为只能包含<li>元素
7、要对HTML内容中特殊字符使用字符实体
