<div>元素用于将相关的元素归组为一个逻辑区，可以将需要共同样式的元素归组
<span>元素用于将相关的内联元素和文本归组在一起
width属性只指定内容区的宽度
text-align属性对块元素中的所有内联内容对齐，直接在内联元素上使用不起作用
可以使用子孙选择器选择嵌套在其他元素中的元素
有些元素有不同的状态，如<a>元素有link、visited、hover、focus、active
可以用伪类单独为各个状态指向样式
