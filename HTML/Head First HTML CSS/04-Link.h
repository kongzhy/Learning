域名是一个唯一的名字，用来唯一标识网站
<a>元素的href属性中可以使用相对路径或者URL来链接其他Web页面。对于网站中的其他页面，最好使用相对路径，对外部链接使用URL
为了便于访问，可以在<a>元素中使用title属性提供链接的一个描述
可以使用id属性在页面中创建一个目标。使用#后面加一个目标id，可以链接到页面中的那个位置
使用target属性在新浏览器窗口中打开链接，使用相同target名的所有链接都会在同一窗口打开，_blank则始终使用一个新窗口
