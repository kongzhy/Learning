// List

// 获得List元素的个数
len(list)

// 使用索引访问List中相应的元素，可以使用-1作为索引，直接获取最后一个元素的索引
List[-1]

// 追加元素到List末尾
append(element)

// 插入元素到List指定位置
insert(index, element)

// 删除List末尾的元素，或删除指定位置的元素
pop()


// Tuple 不可变的List，更安全
// 定义时元素必须被确定
// 只有一个元素的Tuple定义时必须加，消除歧义
