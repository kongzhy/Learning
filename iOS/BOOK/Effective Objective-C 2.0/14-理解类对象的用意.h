Metaclass
每一个OC对象实例都是指向某块内存数据的指针
对象结构体的首个成员是Class类的变量，该变量定义了对象所属的类，称为isa指针
每个实例都有一个指向类对象的指针，用以表明其类型，这些类对象构成了类的继承体系
类对象所属的类型(isa指针指向的类型)是另外一个类，叫做元类，用来表述类对象本身所具备的元数据
编译器假定id类型的对象能响应所有的消息
对象类型并非在编译期就绑定好了，而是要在运行期查找，应该使用类型信息查询方法来探知
isMemberOfClass:判断对象是否为某个特定类的实例
isKindOfClass:判断对象是否为某类或者其派生类的实例
类对象是单例(singleton)
